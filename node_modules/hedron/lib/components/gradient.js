var _templateObject = babelHelpers.taggedTemplateLiteral(['\n  ', '\n  content: \'', '\';\n'], ['\n  ', '\n  content: \'', '\';\n']);

/* eslint-disable no-unused-vars */
import React from 'react';
import styled from 'styled-components';
import { randomGradientName } from '../gradients';
import generator from '../generator';

function GradientContainer(props) {
  var gradient = props.gradient,
      angle = props.angle,
      rest = babelHelpers.objectWithoutProperties(props, ['gradient', 'angle']);

  return React.createElement('div', rest);
}

GradientContainer.propTypes = {
  className: React.PropTypes.string,
  gradient: React.PropTypes.string,
  angle: React.PropTypes.number
};

GradientContainer.defaultProps = {
  gradient: randomGradientName(),
  angle: -90
};

var Gradient = styled(GradientContainer)(_templateObject, function (props) {
  return props ? generator(props) : generator();
}, function (props) {
  return props.name;
});

export default Gradient;