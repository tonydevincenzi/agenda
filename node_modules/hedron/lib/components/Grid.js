var _templateObject = babelHelpers.taggedTemplateLiteral(['\n  display: grid;\n  grid-template-columns: ', ';\n'], ['\n  display: grid;\n  grid-template-columns: ', ';\n']);

/* eslint-disable no-unused-vars */
import React from 'react';
import styled from 'styled-components';
import { divvy } from '../utils';

function GridContainer(props) {
  var divisions = props.divisions,
      rest = babelHelpers.objectWithoutProperties(props, ['divisions']);

  return React.createElement('section', rest);
}

GridContainer.propTypes = {
  className: React.PropTypes.string,
  divisions: React.PropTypes.number
};

GridContainer.defaultProps = {
  divisions: 12
};

var Grid = styled(GridContainer)(_templateObject, function (props) {
  return props.divisions;
});

export default Grid;